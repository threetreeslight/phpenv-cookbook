export PHPENV_ROOT=<%= @rbenv_root %>

case $SHELL in
  */zsh)
    source $PHPENV_ROOT/completions/rbenv.zsh
    ;;
  */bash)
    source $PHPENV_ROOT/completions/rbenv.bash
    ;;
  *)
    source $PHPENV_ROOT/completions/rbenv.bash
    ;;
esac

eval "$(rbenv init -)"
